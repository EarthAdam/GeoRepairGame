using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurtomRayStuff : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    GameObject lastTile;
    LineRenderer line;
    // Update is called once per frame
    void Update()
    {
        
    }
}
